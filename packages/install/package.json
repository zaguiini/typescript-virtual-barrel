{
  "type": "module",
  "name": "@typescript-virtual-barrel/install",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "@typescript-virtual-barrel/install": "./dist/index.js"
  },
  "files": [
    "dist/**"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "TIMING=1 eslint src/**/*.ts* --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^18.7.1",
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "tsconfig": "*"
  },
  "dependencies": {
    "comment-json": "^4.2.3",
    "ora": "^6.3.1",
    "typescript": "^5.4.2"
  }
}
